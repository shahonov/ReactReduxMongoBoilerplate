const validateNonEmptyInputs = (values, names) => {
    const errors = {};
    return errors;
}

const validateNumberInputs = (values, names) => {
    const errors = {};
    return errors;
}

const validateEmailInputs = (values, names) => {
    const errors = {};
    return errors;
}
